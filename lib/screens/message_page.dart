import 'package:flutter/cupertino.dart';

//Refrescar al entrar en la página + swipe down
// Cambiar icono mensaje (1, 2, 3, +) contando mensajes
//Cambiar icono appBar al entrar en la página o quitarlo

class MessagePage extends StatelessWidget {
  const MessagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
